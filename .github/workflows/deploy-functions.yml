name: Deploy Supabase Functions
on:
  push:
    branches: [main]
    paths: ['supabase/functions/**']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: supabase/setup-cli@v1
        with:
          version: latest
          
      - name: Deploy functions
        run: |
          supabase functions deploy generate_summary --project-ref llsjysvklkohnzgmpyob
          supabase functions deploy telegram_webhook --project-ref llsjysvklkohnzgmpyob
          supabase functions deploy ingest_telegram_message --project-ref llsjysvklkohnzgmpyob
          supabase functions deploy ingest_telegram_saved --project-ref llsjysvklkohnzgmpyob
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }} 